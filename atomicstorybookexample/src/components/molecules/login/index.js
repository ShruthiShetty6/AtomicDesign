import React from "react";
import Button from "../../atoms/button";
// import "./style.css";

const Login = (props) => {
  return (
    <div>
      <Button>Sign In</Button>
    </div>
  );
};

export default Login;
